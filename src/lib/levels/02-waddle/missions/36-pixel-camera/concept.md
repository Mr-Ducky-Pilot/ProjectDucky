### Lists that grow

`frames = []` is an **empty** list. Each `frames.append(thing)` adds one more.
By the time you press B, you have a list of every frame you snapped — ready to
play.

This pattern (init empty list → append in a loop → iterate later) is how every
note-taking app, every photo gallery, every chat history works.
