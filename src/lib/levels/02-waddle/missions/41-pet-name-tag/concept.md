### Code that knows about *you*

So far your code has been generic. This one is **personalised** — the browser
reads your duck's name from the pet store and bakes it into the Python before
flashing.

It's the first taste of "your code on your duck" instead of just generic
templates. Level 3 takes this further: every personality choice you make gets
saved and used by the firmware.
