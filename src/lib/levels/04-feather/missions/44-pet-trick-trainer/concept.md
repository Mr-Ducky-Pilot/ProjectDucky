### Trigger → reward

Real dog training works the same way: gesture → behaviour → praise. With
code, the praise is built into the behaviour itself (your duck doesn't crave
treats).

Each saved trick lives in the pet store. Next mission (graduation) reads it
all back and runs a demo of every trick your duck has learned.
