The full game loop is below. Pieces to play with:

- `WALLS` — the level layout
- `WIN_X`, `WIN_Y` — where the goal is
- `SPEED` — how fast the player moves per tilt unit
- The score logic — add timers, lives, multipliers, whatever you want
