### From data to design

The duck you see on every page is built from a small JSON object: primary
color, secondary color, pattern, accessory. Change the data — the picture
changes everywhere instantly. That's because we use a **parameterised SVG**:
one drawing, many appearances.

This pattern is how big games handle hundreds of character outfits without
shipping a thousand separate images.
